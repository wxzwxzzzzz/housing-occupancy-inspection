# 本体参考手册

> 面向前端开发者的本体系统完整参考。
> 覆盖 7 个服务域的类型体系、REST 接口、动作调用和 TypeScript 实战案例。

## 目录

- [什么是本体](#什么是本体)
- [核心概念](#核心概念)
- [类型映射](#类型映射)
- [REST 接口规范](#rest-接口规范)
- [byteawake-ap-oms — 基础平台](#byteawake-ap-oms--基础平台)
- [byteawake-ap-oql — 查询语言](#byteawake-ap-oql--查询语言)
- [byteawake-ap-arche — 启元](#byteawake-ap-arche--启元)
- [byteawake-ap-approval — 审批流程](#byteawake-ap-approval--审批流程)
- [byteawake-ap-basedoc — 基础档案](#byteawake-ap-basedoc--基础档案)
- [byteawake-ap-resource — 资源排班](#byteawake-ap-resource--资源排班)
- [byteawake-prh — 公租房保障监管](#byteawake-prh--公租房保障监管)
- [实战案例](#实战案例)

---

## 什么是本体

**本体（Ontology）** 是 OMS 平台中对业务领域的声明式描述——一套**数据模型 + 行为规则**的 DSL，用 XML 定义，驱动运行时自动生成：

- **数据库表结构** — 实体对应物理表，属性对应字段
- **TypeScript 类型** — 前端直接 import 使用，类型安全
- **REST 接口** — 增删改查、审批、业务规则全自动生成

**核心思路**：前端通过统一的 `POST /api/v1/ontology/{actionName}` 告诉 OMS "对哪个对象做什么"，OMS 运行时负责执行，返回统一格式的结果。

```
本体 XML → OMS 运行时 → 自动生成 REST API + TS 类型 + 数据库
                ↑
前端 POST /api/v1/ontology/{actionName}
     body: { objectType, payload }
```

## 核心概念

### 服务域与命名空间

每个服务域（serviceDomain）对应一个独立部署的微服务，包含一个或多个命名空间：

| 服务域 | 命名空间 | TS 目录 |
|---|---|---|
| `byteawake-ap-oms` | `cn.byteawake.ap.oms` | `ap/oms/` |
| `byteawake-ap-oql` | `cn.byteawake.ap.oql` | `ap/oql/` |
| `byteawake-ap-arche` | `cn.byteawake.ap.arche` | `ap/arche/` |
| `byteawake-ap-approval` | `cn.byteawake.ap.approval` | `ap/approval/` |
| `byteawake-ap-basedoc` | `cn.byteawake.ap.basedoc` | `ap/basedoc/` |
| `byteawake-ap-resource` | `cn.byteawake.ap.resource` | `ap/resource/` |
| `byteawake-prh` | `cn.byteawake.prh` | `prh/` |

### 本体类型

| 类型 | 说明 | 例子 |
|---|---|---|
| **class** | 实体，有属性和动作 | `User`、`Resident` |
| **enum** | 枚举，有限值集合 | `Gender`、`UserStatus` |
| **struct** | 值类型，嵌入其他 class | `GeoPoint`、`Attachment` |
| **interface** | 混入接口，class implements 获得 | `ITenant`、`IAuditInfo` |
| **typedef** | 带约束的类型别名 | `Phone`、`Email` |

### 接口混入

实体通过 `implements` 获得接口属性，这些属性会自动出现在该实体的 Request/Response 中：

| 接口 | 属性 | 用途 |
|---|---|---|
| `ITenant` | `tenant: string` | 多租户隔离 |
| `IAuditInfo` | `creator`, `createAt`, `modifier`, `modifyAt` | 审计追踪 |
| `ILogicDelete` | `dr: boolean` | 逻辑删除 |
| `IEnable` | `enable`, `enableAt`, `disableAt` | 启用/停用 |
| `ITree` | `name`, `parent`, `level`, `path`, `ordinal`, `isEnd` | 树型结构 |
| `ITimeline` | `startTime`, `endTime` | 时间轴 |
| `ISysPreset` | `isSys`, `sysId` | 系统预置 |

## 类型映射

| XML 类型 | TypeScript | 说明 |
|---|---|---|
| `String` | `string` | — |
| `Text` | `string` | 长文本 |
| `DateTime` / `Timestamp` | `string` | ISO 8601 |
| `Boolean` | `boolean` | — |
| `Integer` / `Long` / `Short` | `number` | — |
| `Decimal(p,s)` | `number` | — |
| `Any` | `unknown` | 动态类型 |
| enum 引用 | 联合类型 | `'MALE' \| 'FEMALE'` |
| class 引用 | `string` | 传 id |

## REST 接口规范

### 统一入口

```
POST /api/v1/ontology/{actionName}
Content-Type: application/json
Authorization: Bearer {token}  (公开接口除外)
```

### 请求格式

**写操作（add/modify/delete/submit/archive/verify/checkin/...）**

```json
{
  "objectType": "cn.byteawake.prh.Resident",
  "payload": { "fullName": "张三", "idCardNo": "..." }
}
```

**查询操作（list/detail/refer/querytree）** — payload 为 QuerySpec：

```json
{
  "objectType": "cn.byteawake.prh.Resident",
  "payload": {
    "selection": "fullName,idCardNo,status",
    "filter": { "operator": "AND", "conditions": [...] },
    "sortItems": [{ "path": "fullName", "direction": "ASC" }],
    "page": { "pageNo": 1, "pageSize": 20 }
  }
}
```

**无参数操作（delete/enable/stop/submit/activate/terminate/...）**

```json
{ "objectType": "cn.byteawake.prh.Resident", "id": "object-id" }
```

### 响应格式

**成功：**

```json
{
  "success": true,
  "code": "OK",
  "data": [{ "id": "...", "fullName": "张三", ... }],
  "page": { "pageNo": 1, "pageSize": 20, "total": 156 }
}
```

**校验失败：**

```json
{
  "success": false,
  "code": "VALIDATION_ERROR",
  "message": "提交校验失败",
  "fieldErrors": [
    { "path": "cn.byteawake.prh.Resident.idCardNo", "message": "该身份证号已注册" }
  ]
}
```

---

## byteawake-ap-oms — 基础平台

> 被所有其他服务域 import。定义基础类型、公共接口、动作类型。
> 命名空间：`cn.byteawake.ap.oms` | TS：`ap/oms/`

### 枚举

#### ImportMode — 导入模式

| 名称 | code | 标题 |
|---|---|---|
| `UPSERT` | 10 | 新增或更新 |
| `INSERT_ONLY` | 20 | 仅新增 |
| `UPDATE_ONLY` | 30 | 仅更新 |
| `VALIDATE_ONLY` | 40 | 仅校验 |

#### FenceType — 围栏类型

| 名称 | code | 标题 |
|---|---|---|
| `CIRCLE` | 10 | 圆形 |
| `POLYGON` | 20 | 多边形 |

#### OntologyLevelKind — 级别

| 名称 | code | 标题 |
|---|---|---|
| `INFO` | 10 | 提示 |
| `WARN` | 20 | 警告 |
| `ERROR` | 30 | 错误 |
| `FATAL` | 40 | 严重错误 |

#### TimeGrainKind — 时间粒度

| 名称 | code | 标题 |
|---|---|---|
| `DAY` | 10 | 日 |
| `WEEK` | 20 | 周 |
| `MONTH` | 30 | 月 |
| `QUARTER` | 40 | 季 |
| `YEAR` | 50 | 年 |

#### AdditivityKind — 可加性

| 名称 | code | 标题 |
|---|---|---|
| `ADDITIVE` | 10 | 完全可加 |
| `SEMI_ADDITIVE` | 20 | 半可加 |
| `NON_ADDITIVE` | 30 | 不可加 |

#### DataSetKind — 数据集类别

| 名称 | code | 标题 |
|---|---|---|
| `ONTOLOGY_OBJECT` | 10 | 本体对象 |
| `PHYSICAL_TABLE` | 20 | 物理表 |

#### EnumMemberAliasKind — 别名类别

| 名称 | code | 标题 |
|---|---|---|
| `LEGACY` | 10 | 历史别名 |
| `INPUT` | 20 | 输入别名 |
| `EXTERNAL` | 30 | 外部别名 |
| `SYMBOL` | 40 | 符号别名 |

### Typedef

| 名称 | 标题 | 基础类型 | 约束 |
|---|---|---|---|
| `Phone` | 手机号 | `String` | length=20, `^1[3-9][0-9]{9}$` |
| `Email` | 邮箱 | `String` | length=100, `^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$` |

### Struct

#### GeoPoint — 地理坐标

| 属性 | 标题 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `longitude` | 经度 | `number` | — | precision=10, scale=6 |
| `latitude` | 纬度 | `number` | — | precision=10, scale=6 |

#### Attachment — 附件

| 属性 | 标题 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `name` | 文件名 | `string` | — | max=256 |
| `url` | URL | `string` | — | max=512 |
| `mimeType` | MIME类型 | `string` | — | max=64 |

### Interface

#### ILogicDelete — 逻辑删除

| 属性 | 标题 | 类型 | 默认值 |
|---|---|---|---|
| `dr` | 逻辑删除标记 | `boolean` | `false` |

#### ITenant — 租户

| 属性 | 标题 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `tenant` | 租户 | `string` | ✓ | 分区键, length=36 |

#### ITree — 树型结构

| 属性 | 标题 | 类型 | 默认值 |
|---|---|---|---|
| `name` | 名称 | `string` | — |
| `parent` | 上级 | `string` | — |
| `level` | 层级 | `number` | — |
| `path` | 路径 | `string` | — |
| `ordinal` | 序号 | `number` | — |
| `isEnd` | 是否末级 | `boolean` | `true` |

#### IAuditInfo — 审计信息

| 属性 | 标题 | 类型 |
|---|---|---|
| `creator` | 创建人 | `string` |
| `createAt` | 创建时间 | `string` |
| `modifier` | 修改人 | `string` |
| `modifyAt` | 修改时间 | `string` |

#### IEnable — 启用

| 属性 | 标题 | 类型 | 默认值 |
|---|---|---|---|
| `enable` | 启用 | `boolean` | `false` |
| `enableAt` | 启用时间 | `string` | — |
| `disableAt` | 停用时间 | `string` | — |

#### ITimeline — 时间轴

| 属性 | 标题 | 类型 | field |
|---|---|---|---|
| `startTime` | 生效时间 | `string` | start_time |
| `endTime` | 失效时间 | `string` | end_time |

#### ISysPreset — 系统预置

| 属性 | 标题 | 类型 | 默认值 |
|---|---|---|---|
| `isSys` | 是否系统预置 | `boolean` | `false` |
| `sysId` | 系统标识 | `string` | — |

### Class

#### OntologyObject — 本体对象（抽象）

所有实体的根类型，不可直接实例化。

| 属性 | 标题 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `id` | 标识 | `string` | — | 主键, length=32, isPrimaryKey |
| `pubts` | 时间戳 | `string` | — | — |

#### OntologyResult — 操作结果

所有动作的统一返回类型。

| 属性 | 标题 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `success` | 是否成功 | `boolean` | ✓ | — |
| `code` | 结果编码 | `string` | ✓ | 如 "OK"、"VALIDATION_ERROR" |
| `message` | 结果消息 | `string` | — | 人类可读 |
| `detailMsg` | 详细消息 | `string` | — | — |
| `level` | 结果级别 | `OntologyLevelKind` | — | INFO/WARN/ERROR/FATAL |
| `traceId` | 追踪标识 | `string` | — | 用于日志追踪 |

链接关系：`data` → `OntologyObject[]`，`fieldErrors` → `FieldError[]`

#### FieldError — 字段错误

| 属性 | 标题 | 类型 | 必填 |
|---|---|---|---|
| `path` | 字段路径 | `string` | ✓ |
| `message` | 错误消息 | `string` | ✓ |
| `level` | 错误级别 | `OntologyLevelKind` | — |

#### NavigationSpec — 导航声明（继承 QuerySpec）

| 属性 | 标题 | 类型 | 说明 |
|---|---|---|---|
| `currentId` | 当前对象标识 | `string` | 用于上/下一条导航 |

#### ImportRequest — 导入请求

| 属性 | 标题 | 类型 | 必填 |
|---|---|---|---|
| `fileToken` | 文件令牌 | `string` | ✓ |
| `mode` | 导入模式 | `ImportMode` | ✓ |
| `templateCode` | 模板编码 | `string` | — |

#### Fence — 电子围栏

dataSet: `oms_fence` | implements: `ILogicDelete`, `ITenant`, `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `fenceType` | 围栏类型 | `FenceType` | ✓ | 自有 |
| `center` | 中心坐标 | `GeoPoint` | — | 自有 |
| `radius` | 半径 | `number` | — | 自有 |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |

链接：`vertices` → `FenceVertex[]`

#### FenceVertex — 围栏顶点

dataSet: `oms_fence_vertex` | implements: `ITenant`, `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `fence` | 所属围栏 | `Fence` | ✓ | 自有 |
| `ordinal` | 顶点序号 | `number` | ✓ | 自有 |
| `point` | 顶点坐标 | `GeoPoint` | ✓ | 自有 |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |

---
## byteawake-ap-oql — 查询语言

> 定义查询构建器所需的类型：QuerySpec、过滤条件、排序、分页等。
> 命名空间：`cn.byteawake.ap.oql` | TS：`ap/oql/`
> 所有 class 均为非持久化对象（`isPersistent="false"`），不对应数据库表，仅用于构建请求。

### 枚举

#### QueryLogicalOperator — 查询逻辑操作符

| 名称 | code | 标题 |
|---|---|---|
| `AND` | 10 | 且 |
| `OR` | 20 | 或 |

#### QueryComparisonOperator — 查询比较操作符

| 名称 | code | 标题 |
|---|---|---|
| `EQ` | 10 | 等于 |
| `NE` | 20 | 不等于 |
| `GT` | 30 | 大于 |
| `GE` | 40 | 大于等于 |
| `LT` | 50 | 小于 |
| `LE` | 60 | 小于等于 |
| `LIKE` | 70 | 包含 |
| `IN` | 80 | 包含于 |
| `BETWEEN` | 90 | 区间 |
| `IS_NULL` | 100 | 为空 |
| `NOT_NULL` | 110 | 不为空 |

#### QuerySortDirection — 查询排序方向

| 名称 | code | 标题 |
|---|---|---|
| `ASC` | 10 | 升序 |
| `DESC` | 20 | 降序 |

#### TreeQueryScope — 树查询范围

| 名称 | code | 标题 |
|---|---|---|
| `ROOT_CHILDREN` | 10 | 根节点子级 |
| `SUBTREE` | 20 | 子树 |
| `ANCESTORS` | 30 | 祖先路径 |

### Class

#### QuerySpec — 查询声明

| 属性 | 标题 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `objectType` | 目标本体 | `string` | ✓ | 如 `"cn.byteawake.prh.Resident"` |
| `selection` | 查询字段 | `string` | — | 逗号分隔，如 `"fullName,idCardNo,status"` |
| `filter` | 过滤条件 | `QueryFilterGroup` | — | — |
| `metrics` | 聚合指标 | `string` | — | 逗号分隔 |
| `dimensions` | 分组维度 | `string` | — | 逗号分隔 |
| `having` | 聚合后过滤 | `QueryFilterGroup` | — | — |

链接：`sortItems` → `QuerySortItem[]`，`page` → `QueryPage`，`treeOptions` → `TreeQueryOptions`

#### QueryFilterGroup — 查询条件组

| 属性 | 标题 | 类型 | 必填 |
|---|---|---|---|
| `operator` | 逻辑操作符 | `QueryLogicalOperator` | ✓ |

链接：`conditions` → `QueryFilterCondition[]`，`groups` → `QueryFilterGroup[]`（递归嵌套）

#### QueryFilterCondition — 查询条件项

| 属性 | 标题 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `field` | 查询路径 | `string` | ✓ | 如 `"fullName"`、`"status"` |
| `operator` | 比较操作符 | `QueryComparisonOperator` | ✓ | — |
| `value1` | 条件值1 | `string` | — | EQ/LIKE 等使用 |
| `value2` | 条件值2 | `string` | — | BETWEEN 使用 |
| `values` | 条件值列表 | `unknown[]` | — | IN 使用 |

#### QuerySortItem — 查询排序项

| 属性 | 标题 | 类型 | 必填 |
|---|---|---|---|
| `path` | 查询路径 | `string` | ✓ |
| `direction` | 排序方向 | `QuerySortDirection` | ✓ |

#### QueryPage — 查询分页

| 属性 | 标题 | 类型 | 说明 |
|---|---|---|---|
| `pageNo` | 页码 | `number` | 从 1 开始 |
| `pageSize` | 页大小 | `number` | 默认 20 |
| `first` | 向前条数 | `number` | 游标分页 |
| `after` | 起始游标 | `string` | 游标分页 |
| `last` | 向后条数 | `number` | 游标分页 |
| `before` | 结束游标 | `string` | 游标分页 |

#### TreeQueryOptions — 树查询选项

| 属性 | 标题 | 类型 | 默认值 |
|---|---|---|---|
| `rootId` | 根节点标识 | `string` | — |
| `parentId` | 父节点标识 | `string` | — |
| `maxDepth` | 最大层级 | `number` | — |
| `includeRoot` | 是否包含根节点 | `boolean` | `false` |
| `recursive` | 是否递归 | `boolean` | `true` |
| `scope` | 查询范围 | `TreeQueryScope` | — |

#### ActionSpec — 动作声明

| 属性 | 标题 | 类型 | 必填 |
|---|---|---|---|
| `objectType` | 目标本体 | `string` | ✓ |
| `actionName` | 动作名称 | `string` | ✓ |

链接：`payload` → `OntologyObject`

---
## byteawake-ap-arche — 启元

> 统一账户、企业、租户管理平台。核心实体为 User、Enterprise、Tenant。
> 命名空间：`cn.byteawake.ap.arche` | TS：`ap/arche/`

### 枚举

#### UserStatus — 用户状态

| 名称 | code | 标题 |
|---|---|---|
| `INACTIVE` | 10 | 未激活 |
| `ACTIVE` | 20 | 正常 |
| `DISABLED` | 30 | 已停用 |
| `CANCELLED` | 40 | 已注销 |
| `LOCKED` | 50 | 已锁定 |

#### UserType — 用户类型

| 名称 | code | 标题 |
|---|---|---|
| `PLATFORM` | 10 | 平台用户 |
| `ENTERPRISE` | 20 | 企业用户 |
| `ANONYMOUS` | 30 | 匿名用户 |

#### EnterpriseState — 企业状态

| 名称 | code | 标题 |
|---|---|---|
| `UNAUTHENTICATED` | 10 | 未认证 |
| `PENDING` | 20 | 认证中 |
| `APPROVED` | 30 | 已认证 |
| `REJECTED` | 40 | 认证驳回 |

#### TenantStatus — 租户状态

| 名称 | code | 标题 |
|---|---|---|
| `TRIAL` | 10 | 试用 |
| `ACTIVE` | 20 | 正常 |
| `SUSPENDED` | 30 | 欠费停用 |
| `TERMINATED` | 40 | 已终止 |

#### EmailChangeStatus — 邮箱变更状态

| 名称 | code | 标题 |
|---|---|---|
| `NONE` | 10 | 无变更 |
| `PENDING` | 20 | 待确认 |
| `CONFIRMED` | 30 | 已确认 |

### Class

#### User — 用户

extends: `OntologyObject` | implements: `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `account` | 登录账号 | `string` | ✓ | 自有 |
| `realName` | 真实姓名 | `string` | — | 自有 |
| `nickname` | 昵称 | `string` | — | 自有 |
| `phone` | 手机号 | `Phone` | — | 自有 |
| `email` | 邮箱 | `Email` | — | 自有 |
| `avatar` | 头像 | `string` | — | 自有 |
| `status` | 状态 | `UserStatus` | ✓ | 自有 |
| `userType` | 用户类型 | `UserType` | ✓ | 自有 |
| `activateAt` | 激活时间 | `string` | — | 自有 |
| `disableAt` | 停用时间 | `string` | — | 自有 |
| `deleteAt` | 注销时间 | `string` | — | 自有 |
| `failedLoginAttempts` | 连续登录失败次数 | `number` | — | 自有 |
| `lockedUntil` | 锁定截止时间 | `string` | — | 自有 |
| `isAnonymous` | 是否匿名用户 | `boolean` | ✓ | 自有, 默认 `false` |
| `isSSOUser` | 是否SSO用户 | `boolean` | ✓ | 自有, 默认 `false` |
| `emailConfirmedAt` | 邮箱确认时间 | `string` | — | 自有 |
| `phoneConfirmedAt` | 手机确认时间 | `string` | — | 自有 |
| `lastSignInAt` | 最后登录时间 | `string` | — | 自有 |
| `bannedUntil` | 封禁截止时间 | `string` | — | 自有 |
| `confirmedAt` | 确认时间 | `string` | — | 自有 |
| `rawAppMetaData` | 应用元数据 | `string` | — | 自有 |
| `rawUserMetaData` | 用户元数据 | `string` | — | 自有 |
| `emailChangeStatus` | 邮箱变更状态 | `EmailChangeStatus` | ✓ | 自有, 默认 `NONE` |
| `emailChangeTarget` | 邮箱变更目标 | `string` | — | 自有 |
| `invitedAt` | 邀请时间 | `string` | — | 自有 |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |

#### User 动作

##### signup — 注册

`POST /api/v1/ontology/signup`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.account` | `string` | ✓ | 登录账号, `[a-zA-Z][a-zA-Z0-9_]{3,59}` |
| `payload.email` | `Email` | ✓ | 邮箱 |
| `payload.password` | `string` | ✓ | 密码 |
| `payload.confirmPassword` | `string` | ✓ | 确认密码 |
| `payload.phone` | `Phone` | — | 手机号（短信注册时必填） |
| `payload.provider` | `string` | — | 联邦身份提供者（联邦注册时必填） |
| `payload.providerId` | `string` | — | 联邦身份ID（联邦注册时必填） |
| `payload.rawUserMetaData` | `string` | — | 用户元数据 |

**Response:**

| 字段 | 类型 | 说明 |
|---|---|---|
| `success` | `boolean` | 是否成功 |
| `data[0].userId` | `string` | 新用户 ID |
| `data[0].sessionId` | `string` | 会话 ID |
| `data[0].sessionAal` | `number` | 认证保证级别 |

##### authenticate — 身份认证

`POST /api/v1/ontology/authenticate`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.account` | `string` | — | 登录账号（三选一） |
| `payload.email` | `Email` | — | 邮箱（三选一） |
| `payload.phone` | `Phone` | — | 手机号（三选一） |
| `payload.password` | `string` | ✓ | 密码 |

**Response:**

| 字段 | 类型 | 说明 |
|---|---|---|
| `success` | `boolean` | 是否成功 |
| `data[0].userId` | `string` | 用户 ID |
| `data[0].userStatus` | `UserStatus` | 用户状态 |
| `data[0].sessionId` | `string` | 会话 ID |
| `data[0].sessionAal` | `number` | 认证保证级别 |

##### signInAnonymously — 匿名登录

`POST /api/v1/ontology/signInAnonymously`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |

**Response:** 标准 OntologyResult，data 包含 userId、sessionId

##### recover — 密码恢复

`POST /api/v1/ontology/recover`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.email` | `Email` | ✓ | 注册邮箱 |

**Response:** 标准 OntologyResult

##### sendOTP — 发送验证码

`POST /api/v1/ontology/sendOTP`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.phone` | `Phone` | — | 手机号（与 email 二选一） |
| `payload.email` | `Email` | — | 邮箱（与 phone 二选一） |

**Response:** 标准 OntologyResult

##### sendMagicLink — 发送魔法链接

`POST /api/v1/ontology/sendMagicLink`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.email` | `Email` | ✓ | 邮箱 |

**Response:** 标准 OntologyResult

##### resendConfirmation — 重发确认

`POST /api/v1/ontology/resendConfirmation`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.type` | `string` | ✓ | 重发类型 |
| `payload.email` | `Email` | — | 邮箱 |

**Response:** 标准 OntologyResult

##### reauthenticate — 重新认证

`POST /api/v1/ontology/reauthenticate`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.password` | `string` | ✓ | 当前密码 |

**Response:** 标准 OntologyResult

##### enableUser — 启用

`POST /api/v1/ontology/enableUser`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `id` | `string` | ✓ | 用户 ID |

**Response:** 标准 OntologyResult

##### disable — 停用

`POST /api/v1/ontology/disable`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `id` | `string` | ✓ | 用户 ID |

**Response:** 标准 OntologyResult

##### deleteUser — 注销用户

`POST /api/v1/ontology/deleteUser`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `id` | `string` | ✓ | 用户 ID |

**Response:** 标准 OntologyResult

##### unlockUser — 解锁

`POST /api/v1/ontology/unlockUser`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `id` | `string` | ✓ | 用户 ID |

**Response:** 标准 OntologyResult

##### changeProfile — 修改资料

`POST /api/v1/ontology/changeProfile`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.realName` | `string` | — | 真实姓名 |
| `payload.nickname` | `string` | — | 昵称 |
| `payload.avatar` | `string` | — | 头像 |

**Response:** 标准 OntologyResult

##### changePhone — 更换手机号

`POST /api/v1/ontology/changePhone`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.phone` | `Phone` | ✓ | 新手机号 |

**Response:** 标准 OntologyResult

##### bindEmail — 绑定邮箱

`POST /api/v1/ontology/bindEmail`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.email` | `Email` | ✓ | 邮箱 |

**Response:** 标准 OntologyResult

##### changeEmail — 修改邮箱

`POST /api/v1/ontology/changeEmail`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.email` | `Email` | ✓ | 新邮箱 |

**Response:** 标准 OntologyResult

##### confirmEmailChange — 确认邮箱变更

`POST /api/v1/ontology/confirmEmailChange`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.token` | `string` | ✓ | 确认令牌 |

**Response:** 标准 OntologyResult

##### inviteUser — 邀请用户

`POST /api/v1/ontology/inviteUser`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.email` | `Email` | ✓ | 被邀请者邮箱 |

**Response:** 标准 OntologyResult

##### bindIdentity — 绑定联邦身份

`POST /api/v1/ontology/bindIdentity`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.provider` | `string` | ✓ | 身份提供者 |
| `payload.providerId` | `string` | ✓ | 身份ID |

**Response:** 标准 OntologyResult

##### detail — 详情

`POST /api/v1/ontology/detail`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `id` | `string` | ✓ | 用户 ID |

**Response:** 标准 OntologyResult，data 包含完整 User 对象

##### list — 列表

`POST /api/v1/ontology/list`

**Request Body:** 标准 QuerySpec（objectType = `"cn.byteawake.ap.arche.User"`）

**Response:** 标准 OntologyResult + 分页信息

##### listdetail — 列表（带凭据）

`POST /api/v1/ontology/listdetail`

**Request Body:** 标准 QuerySpec（objectType = `"cn.byteawake.ap.arche.User"`）

**Response:** 标准 OntologyResult + 分页信息，data 包含关联凭据信息

##### adminCreateUser — 管理员创建用户

`POST /api/v1/ontology/adminCreateUser`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.account` | `string` | ✓ | 登录账号 |
| `payload.email` | `Email` | — | 邮箱 |
| `payload.phone` | `Phone` | — | 手机号 |
| `payload.password` | `string` | ✓ | 初始密码 |

**Response:** 标准 OntologyResult

##### adminGenerateLink — 管理员生成链接

`POST /api/v1/ontology/adminGenerateLink`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.type` | `string` | ✓ | 链接类型（signup/magiclink） |
| `payload.email` | `Email` | — | 目标邮箱 |

**Response:** 标准 OntologyResult，data 包含生成的链接 URL

##### authenticateWeb3 — Web3钱包认证

`POST /api/v1/ontology/authenticateWeb3`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `payload.walletAddress` | `string` | ✓ | 钱包地址 |
| `payload.signature` | `string` | ✓ | 签名 |

**Response:** 标准 OntologyResult

##### lockAccount — 锁定账户

`POST /api/v1/ontology/lockAccount`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `id` | `string` | ✓ | 用户 ID |

**Response:** 标准 OntologyResult

##### unlockAccount — 解锁账户

`POST /api/v1/ontology/unlockAccount`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.User"` |
| `id` | `string` | ✓ | 用户 ID |

**Response:** 标准 OntologyResult

#### Enterprise — 企业

extends: `OntologyObject` | implements: `IAuditInfo`, `ILogicDelete`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `name` | 企业名称 | `string` | ✓ | 自有 |
| `integrationCode` | 统一社会信用代码 | `string` | — | 自有 |
| `creditCode` | 信用代码 | `string` | — | 自有 |
| `legalPerson` | 法人代表 | `string` | — | 自有 |
| `registerAddress` | 工商注册地址 | `string` | — | 自有 |
| `contactName` | 联系人 | `string` | — | 自有 |
| `contactPhone` | 联系人手机 | `Phone` | — | 自有 |
| `contactEmail` | 联系人邮箱 | `Email` | — | 自有 |
| `state` | 状态 | `EnterpriseState` | ✓ | 自有 |
| `sourceSystemId` | 来源系统 | `string` | — | 自有 |
| `logo` | Logo | `string` | — | 自有 |
| `scale` | 企业规模 | `string` | — | 自有 |
| `website` | 企业官网 | `string` | — | 自有 |
| `industryId` | 所属行业 | `string` | — | 自有 |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |

#### Enterprise 动作

##### createEnterprise — 创建企业

`POST /api/v1/ontology/createEnterprise`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.Enterprise"` |
| `payload.name` | `string` | ✓ | 企业名称 |
| `payload.integrationCode` | `string` | — | 统一社会信用代码 |
| `payload.creditCode` | `string` | — | 信用代码 |
| `payload.legalPerson` | `string` | — | 法人代表 |
| `payload.contactName` | `string` | — | 联系人 |
| `payload.contactPhone` | `Phone` | — | 联系人手机 |
| `payload.contactEmail` | `Email` | — | 联系人邮箱 |
| `payload.industryId` | `string` | — | 所属行业 |

**Response:** 标准 OntologyResult

##### updateEnterprise — 修改企业

`POST /api/v1/ontology/updateEnterprise`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.arche.Enterprise"` |
| `payload.name` | `string` | ✓ | 企业名称 |
| `payload.contactName` | `string` | — | 联系人 |
| `payload.contactPhone` | `Phone` | — | 联系人手机 |
| `payload.contactEmail` | `Email` | — | 联系人邮箱 |
| `payload.logo` | `string` | — | Logo |
| `payload.scale` | `string` | — | 企业规模 |
| `payload.website` | `string` | — | 企业官网 |
| `payload.industryId` | `string` | — | 所属行业 |
| `payload.registerAddress` | `string` | — | 工商注册地址 |

**Response:** 标准 OntologyResult

##### submitAuthentication / rejectAuthentication / approveAuthentication

`POST /api/v1/ontology/{actionName}`

| 动作 | 说明 | Request |
|---|---|---|
| `submitAuthentication` | 提交认证 | objectType + id |
| `rejectAuthentication` | 拒绝认证 | objectType + id |
| `approveAuthentication` | 通过认证 | objectType + id |

##### detail / list / listdetail

标准查询动作，objectType = `"cn.byteawake.ap.arche.Enterprise"`

#### Tenant — 租户

extends: `OntologyObject` | implements: `IAuditInfo`, `ILogicDelete`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `name` | 租户名称 | `string` | ✓ | 自有 |
| `enterprise` | 所属企业 | `Enterprise` | ✓ | 自有 |
| `status` | 租户状态 | `TenantStatus` | ✓ | 自有 |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |

Tenant 动作包括：`createTenant`、`updateTenant`、`stopTenant`、`detail`、`list`、`listdetail`。

---
## byteawake-ap-approval — 审批流程

> 审批公共接口定义，被其他服务域的实体 implements 混入。
> 命名空间：`cn.byteawake.ap.approval` | TS：`ap/approval/`
> 本服务域只定义枚举和接口，不包含持久化实体。

### 枚举

#### VerifyState — 审批流状态

| 名称 | code | 标题 |
|---|---|---|
| `PENDING` | 10 | 待审批 |
| `APPROVED` | 20 | 已通过 |
| `REJECTED` | 30 | 已驳回 |
| `RETURNED` | 40 | 已退回 |
| `CANCELLED` | 50 | 已取消 |

#### ApprovalResult — 审批结果

| 名称 | code | 标题 |
|---|---|---|
| `APPROVED` | 10 | 通过 |
| `REJECTED` | 20 | 驳回 |
| `RETURNED` | 30 | 退回 |

### Interface

#### ISubmitInfo — 提交信息

| 属性 | 标题 | 类型 | field |
|---|---|---|---|
| `submittedAt` | 提交时间 | `string` | submitted_at |
| `submittedBy` | 提交人 | `User` | submitted_by_id |
| `withdrawnAt` | 撤回时间 | `string` | withdrawn_at |
| `withdrawnBy` | 撤回人 | `User` | withdrawn_by_id |

#### IApprovalInfo — 审批信息

| 属性 | 标题 | 类型 | field |
|---|---|---|---|
| `approver` | 审批人 | `User` | approver_id |
| `approvalTime` | 审批时间 | `string` | approval_time |
| `approvalOpinion` | 审批意见 | `string` | approval_opinion |
| `approvalResult` | 审批结果 | `ApprovalResult` | approval_result |

#### IApprovalFlow — 审批流信息

| 属性 | 标题 | 类型 | field | 默认值 |
|---|---|---|---|---|
| `approvalFlowEnabled` | 是否启用审批流 | `boolean` | approval_flow_enabled | `false` |
| `verifyState` | 审批流状态 | `number` | verify_state | — |
| `returnCount` | 退回次数 | `number` | return_count | — |
| `processInstance` | 流程实例 | `string` | process_instance_id | — |
| `approvalStep` | 审批环节 | `string` | approval_step | — |
| `pendingApprover` | 待审批人 | `User` | pending_approver_id | — |
| `task` | 任务 | `string` | task_id | — |

---
## byteawake-ap-basedoc — 基础档案

> 基础数据定义，核心实体为行政区划（AdministrativeRegion）。
> 命名空间：`cn.byteawake.ap.basedoc` | TS：`ap/basedoc/`

### 枚举

#### RegionType — 行政区划类型

| 名称 | code | 标题 | 层级 |
|---|---|---|---|
| `PROVINCE` | 11 | 省 | 省级 |
| `AUTONOMOUS_REGION` | 12 | 自治区 | 省级 |
| `MUNICIPALITY` | 13 | 直辖市 | 省级 |
| `SAR` | 14 | 特别行政区 | 省级 |
| `PREFECTURE_CITY` | 21 | 地级市 | 地级 |
| `PREFECTURE` | 22 | 地区 | 地级 |
| `AUTONOMOUS_PREFECTURE` | 23 | 自治州 | 地级 |
| `LEAGUE` | 24 | 盟 | 地级 |
| `DISTRICT` | 31 | 市辖区 | 县级 |
| `COUNTY` | 32 | 县 | 县级 |
| `COUNTY_CITY` | 33 | 县级市 | 县级 |
| `AUTONOMOUS_COUNTY` | 34 | 自治县 | 县级 |
| `BANNER` | 35 | 旗 | 县级 |
| `AUTONOMOUS_BANNER` | 36 | 自治旗 | 县级 |
| `SPECIAL_DISTRICT` | 37 | 特区 | 县级 |
| `FORESTRY_AREA` | 38 | 林区 | 县级 |
| `SUBDISTRICT` | 41 | 街道 | 乡级 |
| `TOWN` | 42 | 镇 | 乡级 |
| `TOWNSHIP` | 43 | 乡 | 乡级 |
| `ETHNIC_TOWNSHIP` | 44 | 民族乡 | 乡级 |
| `SUM` | 45 | 苏木 | 乡级 |
| `ETHNIC_SUM` | 46 | 民族苏木 | 乡级 |
| `NEIGHBORHOOD_COMMITTEE` | 51 | 居委会 | 村级 |
| `VILLAGE_COMMITTEE` | 52 | 村委会 | 村级 |
| `SIMILAR_VILLAGE_UNIT` | 53 | 类似村级单位 | 村级 |

### Class

#### AdministrativeRegion — 行政区划

dataSet: `ap_basedoc_admin_region` | extends: `OntologyObject` | implements: `ITree`, `ITenant`, `IAuditInfo`, `IEnable`, `ISysPreset`, `ILogicDelete`, `ITimeline`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `code` | 编码 | `string` | ✓ | 自有 |
| `nationalCode` | 国标码 | `string` | ✓ | 自有 |
| `countryCode` | 国家码 | `string` | ✓ | 自有 |
| `regionType` | 区划类型 | `RegionType` | ✓ | 自有 |
| `shortName` | 简称 | `string` | — | 自有 |
| `pinyin` | 拼音 | `string` | — | 自有 |
| `pinyinShort` | 拼音缩写 | `string` | — | 自有 |
| `longitude` | 中心点经度 | `number` | — | 自有 |
| `latitude` | 中心点纬度 | `number` | — | 自有 |
| `name` | 名称 | `string` | — | 来自 ITree |
| `parent` | 上级 | `string` | — | 来自 ITree |
| `level` | 层级 | `number` | — | 来自 ITree |
| `path` | 路径 | `string` | — | 来自 ITree |
| `ordinal` | 序号 | `number` | — | 来自 ITree |
| `isEnd` | 是否末级 | `boolean` | — | 来自 ITree, 默认 `true` |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |
| `enable` | 启用 | `boolean` | — | 来自 IEnable, 默认 `false` |
| `enableAt` | 启用时间 | `string` | — | 来自 IEnable |
| `disableAt` | 停用时间 | `string` | — | 来自 IEnable |
| `isSys` | 是否系统预置 | `boolean` | — | 来自 ISysPreset, 默认 `false` |
| `sysId` | 系统标识 | `string` | — | 来自 ISysPreset |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete, 默认 `false` |
| `startTime` | 生效时间 | `string` | — | 来自 ITimeline |
| `endTime` | 失效时间 | `string` | — | 来自 ITimeline |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |

#### AdministrativeRegion 动作

##### add — 新增区划

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.basedoc.AdministrativeRegion"` |
| `payload.code` | `string` | ✓ | 编码 |
| `payload.nationalCode` | `string` | ✓ | 国标码 |
| `payload.countryCode` | `string` | ✓ | 国家码 |
| `payload.regionType` | `RegionType` | ✓ | 区划类型 |
| `payload.shortName` | `string` | — | 简称 |
| `payload.pinyin` | `string` | — | 拼音 |
| `payload.pinyinShort` | `string` | — | 拼音缩写 |
| `payload.longitude` | `number` | — | 中心点经度 |
| `payload.latitude` | `number` | — | 中心点纬度 |

**Response:** 标准 OntologyResult

##### modify — 修改区划

`POST /api/v1/ontology/modify`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.basedoc.AdministrativeRegion"` |
| `payload.regionType` | `RegionType` | — | 区划类型（code/nationalCode/countryCode 不可改） |
| `payload.shortName` | `string` | — | 简称 |
| `payload.pinyin` | `string` | — | 拼音 |
| `payload.pinyinShort` | `string` | — | 拼音缩写 |
| `payload.longitude` | `number` | — | 中心点经度 |
| `payload.latitude` | `number` | — | 中心点纬度 |

**Response:** 标准 OntologyResult

##### delete — 删除区划

`POST /api/v1/ontology/delete`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.basedoc.AdministrativeRegion"` |
| `id` | `string` | ✓ | 区划 ID |

**Response:** 标准 OntologyResult

##### list — 列表

`POST /api/v1/ontology/list`

**Request Body:** 标准 QuerySpec（objectType = `"cn.byteawake.ap.basedoc.AdministrativeRegion"`）

**Response:** 标准 OntologyResult + 分页信息

##### detail — 详情

`POST /api/v1/ontology/detail`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.basedoc.AdministrativeRegion"` |
| `id` | `string` | ✓ | 区划 ID |

**Response:** 标准 OntologyResult，data 包含完整对象

##### querytree — 树查询

`POST /api/v1/ontology/querytree`

**Request Body:** 标准 QuerySpec + TreeQueryOptions（objectType = `"cn.byteawake.ap.basedoc.AdministrativeRegion"`）

**Response:** 标准 OntologyResult，data 为树形结构

##### import — 导入

`POST /api/v1/ontology/import`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.basedoc.AdministrativeRegion"` |
| `payload.fileToken` | `string` | ✓ | 文件令牌 |
| `payload.mode` | `ImportMode` | ✓ | 导入模式 |

**Response:** 标准 OntologyResult

##### enable — 启用

`POST /api/v1/ontology/enable`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.basedoc.AdministrativeRegion"` |
| `id` | `string` | ✓ | 区划 ID |

**Response:** 标准 OntologyResult

##### stop — 停用

`POST /api/v1/ontology/stop`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.basedoc.AdministrativeRegion"` |
| `id` | `string` | ✓ | 区划 ID |

**Response:** 标准 OntologyResult

##### refer — 参照

`POST /api/v1/ontology/refer`

**Request Body:** 标准 QuerySpec（objectType = `"cn.byteawake.ap.basedoc.AdministrativeRegion"`）

**Response:** 标准 OntologyResult + 分页信息

---
## byteawake-ap-resource — 资源排班

> 资源调度与日历管理，对齐 Odoo Resource 模块。
> 命名空间：`cn.byteawake.ap.resource` | TS：`ap/resource/`

### 枚举

#### ResourceType — 资源类型

| 名称 | code | 标题 |
|---|---|---|
| `HUMAN` | 1 | 人员 |
| `MATERIAL` | 2 | 物料 |

#### Weekday — 星期

| 名称 | code | 标题 |
|---|---|---|
| `MONDAY` | 1 | 周一 |
| `TUESDAY` | 2 | 周二 |
| `WEDNESDAY` | 3 | 周三 |
| `THURSDAY` | 4 | 周四 |
| `FRIDAY` | 5 | 周五 |
| `SATURDAY` | 6 | 周六 |
| `SUNDAY` | 7 | 周日 |

#### DayPeriod — 日内时段

| 名称 | code | 标题 |
|---|---|---|
| `MORNING` | 1 | 上午 |
| `BREAK` | 2 | 休息 |
| `AFTERNOON` | 3 | 下午 |
| `FULL_DAY` | 4 | 全天 |

#### WeekType — 双周类型

| 名称 | code | 标题 |
|---|---|---|
| `FIRST` | 0 | 第一周 |
| `SECOND` | 1 | 第二周 |

#### LeaveEffect — 休班效果

| 名称 | code | 标题 |
|---|---|---|
| `TIME_OFF` | 0 | 休班 |
| `MAKE_UP_WORKDAY` | 1 | 补班工作日 |

#### TimeType — 时间类型

| 名称 | code | 标题 |
|---|---|---|
| `LEAVE` | 1 | 缺勤 |
| `OTHER` | 2 | 其他工作安排 |

#### ScheduleType — 调度类型

| 名称 | code | 标题 |
|---|---|---|
| `FIXED` | 1 | 固定 |
| `FLEXIBLE` | 2 | 弹性 |

### Class

#### Resource — 资源

dataSet: `ap_resource_resource` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`, `IEnable`, `ILogicDelete`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `code` | 编码 | `string` | ✓ | 自有 |
| `name` | 名称 | `string` | ✓ | 自有 |
| `resourceType` | 资源类型 | `ResourceType` | ✓ | 自有 |
| `calendar` | 默认日历 | `Calendar` | — | 自有 |
| `timezone` | 时区 | `string` | — | 自有 |
| `efficiencyFactor` | 效率因子 | `number` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |
| `enable` | 启用 | `boolean` | — | 来自 IEnable, 默认 `false` |
| `enableAt` | 启用时间 | `string` | — | 来自 IEnable |
| `disableAt` | 停用时间 | `string` | — | 来自 IEnable |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete, 默认 `false` |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |

#### Resource 动作

##### add — 新增资源

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.resource.Resource"` |
| `payload.code` | `string` | ✓ | 编码 |
| `payload.name` | `string` | ✓ | 名称 |
| `payload.resourceType` | `ResourceType` | ✓ | 资源类型 |
| `payload.calendar` | `Calendar` | — | 默认日历 |
| `payload.timezone` | `string` | — | 时区 |
| `payload.efficiencyFactor` | `number` | — | 效率因子 |

**Response:** 标准 OntologyResult

##### modify — 修改资源

`POST /api/v1/ontology/modify`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.ap.resource.Resource"` |
| `payload.name` | `string` | — | 名称（code 不可改） |
| `payload.resourceType` | `ResourceType` | — | 资源类型 |
| `payload.calendar` | `Calendar` | — | 默认日历 |
| `payload.timezone` | `string` | — | 时区 |
| `payload.efficiencyFactor` | `number` | — | 效率因子 |

**Response:** 标准 OntologyResult

##### delete / list / detail / enable / stop / refer

与 AdministrativeRegion 同模式，objectType = `"cn.byteawake.ap.resource.Resource"`

#### Calendar — 资源日历

dataSet: `ap_resource_calendar` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`, `IEnable`, `ILogicDelete`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `code` | 编码 | `string` | ✓ | 自有 |
| `name` | 名称 | `string` | ✓ | 自有 |
| `timezone` | 时区 | `string` | — | 自有 |
| `hoursPerDay` | 每日小时数 | `number` | — | 自有（自动推算） |
| `hoursPerWeek` | 每周小时数 | `number` | — | 自有（自动推算） |
| `scheduleType` | 调度类型 | `ScheduleType` | ✓ | 自有 |
| `twoWeeksCalendar` | 双周日历 | `boolean` | — | 自有 |
| `description` | 描述 | `string` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |
| `enable` | 启用 | `boolean` | — | 来自 IEnable |
| `enableAt` | 启用时间 | `string` | — | 来自 IEnable |
| `disableAt` | 停用时间 | `string` | — | 来自 IEnable |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |

Calendar 动作：add / modify / delete / list / detail / enable / stop / refer，与 Resource 同模式。

#### CalendarAttendance — 工作时段

dataSet: `ap_resource_calendar_attendance` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`, `IEnable`, `ILogicDelete`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `calendar` | 所属日历 | `Calendar` | ✓ | 自有 |
| `name` | 名称 | `string` | ✓ | 自有 |
| `dayOfWeek` | 星期 | `Weekday` | ✓ | 自有 |
| `hourFrom` | 开始小时 | `number` | ✓ | 自有 |
| `hourTo` | 结束小时 | `number` | ✓ | 自有 |
| `dayPeriod` | 日内时段 | `DayPeriod` | — | 自有 |
| `weekType` | 双周类型 | `WeekType` | — | 自有 |
| `ordinal` | 序号 | `number` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `enable`~`disableAt` | 启用信息 | — | — | 来自 IEnable |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

CalendarAttendance 动作：add / modify / delete / list / detail / enable / stop / refer

#### CalendarLeave — 日历休班

dataSet: `ap_resource_calendar_leave` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`, `IEnable`, `ILogicDelete`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `calendar` | 所属日历 | `Calendar` | — | 自有 |
| `resource` | 所属资源 | `Resource` | — | 自有 |
| `name` | 名称 | `string` | ✓ | 自有 |
| `dateFrom` | 开始时间 | `string` | ✓ | 自有 |
| `dateTo` | 结束时间 | `string` | ✓ | 自有 |
| `effect` | 休班效果 | `LeaveEffect` | ✓ | 自有 |
| `timeType` | 时间类型 | `TimeType` | — | 自有 |
| `description` | 描述 | `string` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `enable`~`disableAt` | 启用信息 | — | — | 来自 IEnable |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

CalendarLeave 动作：add / modify / delete / list / detail / enable / stop / refer

---
## byteawake-prh — 公租房保障监管

> 公租房保障监管系统核心业务域，包含居民、家庭、居住、工作、收入、保障申请、配租、补贴、考勤等实体。
> 命名空间：`cn.byteawake.prh` | TS：`prh/`

### 枚举

#### Gender — 性别

| 名称 | code | 标题 |
|---|---|---|
| `MALE` | 10 | 男 |
| `FEMALE` | 20 | 女 |

#### MaritalStatus — 婚姻状况

| 名称 | code | 标题 |
|---|---|---|
| `UNMARRIED` | 10 | 未婚 |
| `MARRIED` | 20 | 已婚 |
| `DIVORCED` | 30 | 离异 |
| `WIDOWED` | 40 | 丧偶 |

#### GuaranteeType — 保障类型

| 名称 | code | 标题 |
|---|---|---|
| `NEW_EMPLOYEE` | 10 | 新就业无房职工 |
| `MINIMUM_LIVING` | 20 | 城市低保家庭 |
| `EXTREME_POVERTY` | 30 | 特困家庭 |
| `LOW_INCOME` | 40 | 城市低收入家庭 |
| `MIDDLE_INCOME` | 50 | 城市中等收入家庭 |
| `EXTERNAL_WORKER` | 60 | 稳定就业外来务工家庭 |

#### ResidentStatus — 居民状态

| 名称 | code | 标题 |
|---|---|---|
| `DRAFT` | 10 | 草稿 |
| `UNVERIFIED` | 20 | 未认证 |
| `VERIFIED` | 30 | 已认证 |
| `ACTIVATED` | 40 | 已激活 |
| `ARCHIVED` | 50 | 已退出 |

#### HouseholdStatus — 保障家庭状态

| 名称 | code | 标题 |
|---|---|---|
| `DRAFT` | 10 | 草稿 |
| `ACTIVE` | 20 | 已激活 |
| `CANDIDATE` | 30 | 候选 |
| `ARCHIVED` | 40 | 已退出 |

#### RecordStatus — 记录状态

| 名称 | code | 标题 |
|---|---|---|
| `RECORD_ACTIVE` | 10 | 生效 |
| `RECORD_ARCHIVED` | 20 | 归档 |

#### ApplicationStatus — 申请状态

| 名称 | code | 标题 |
|---|---|---|
| `DRAFT` | 10 | 草稿 |
| `UNDER_APPROVAL` | 20 | 审批中 |
| `COMPLETED` | 30 | 已完成 |
| `CANCELLED` | 40 | 已取消 |

#### ApplicationType — 申请类型

| 名称 | code | 标题 |
|---|---|---|
| `INITIAL` | 10 | 首次准入 |
| `REACTIVATION` | 20 | 重新激活 |
| `ANNUAL_REVIEW` | 30 | 年度复审 |

#### AttendanceType — 出勤类型

| 名称 | code | 标题 |
|---|---|---|
| `RESIDENCE` | 10 | 居住出勤 |
| `EMPLOYMENT` | 20 | 工作出勤 |

#### AttendanceStatus — 考勤状态

| 名称 | code | 标题 |
|---|---|---|
| `PENDING` | 10 | 待打卡 |
| `VALID` | 20 | 有效 |
| `INVALID` | 30 | 无效 |
| `MISSED` | 40 | 缺勤 |
| `EXEMPTED` | 50 | 豁免 |

#### AttendanceMode — 考勤打卡方式

| 名称 | code | 标题 |
|---|---|---|
| `MINI_PROGRAM` | 10 | 小程序 |
| `KIOSK` | 20 | 自助终端 |
| `MANUAL` | 30 | 管理员代录 |
| `MAKEUP` | 40 | 补卡 |

#### AttendancePeriod — 考核周期

| 名称 | code | 标题 |
|---|---|---|
| `WEEKLY` | 10 | 每周 |
| `BIWEEKLY` | 20 | 每两周 |
| `MONTHLY` | 30 | 每月 |

#### IncomeType — 收入类型

| 名称 | code | 标题 |
|---|---|---|
| `SALARY` | 10 | 工资薪金 |
| `BUSINESS` | 20 | 经营所得 |
| `PROPERTY` | 30 | 财产性收入 |
| `TRANSFER` | 40 | 转移性收入 |

#### AllocationStatus — 分配状态

| 名称 | code | 标题 |
|---|---|---|
| `DRAFT` | 00 | 草稿 |
| `ALLOC_ACTIVE` | 10 | 生效 |
| `ALLOC_TERMINATED` | 20 | 已终止 |
| `ALLOC_EXPIRED` | 30 | 已到期 |

#### SubsidyStatus — 补贴状态

| 名称 | code | 标题 |
|---|---|---|
| `SUBSIDY_ACTIVE` | 10 | 生效 |
| `SUBSIDY_SUSPENDED` | 20 | 暂停 |
| `SUBSIDY_TERMINATED` | 30 | 已终止 |
| `SUBSIDY_EXPIRED` | 40 | 已到期 |

#### TerminationReason — 退出原因

| 名称 | code | 标题 |
|---|---|---|
| `VOLUNTARY` | 10 | 主动放弃 |
| `INCOME_EXCEED` | 20 | 收入超标 |
| `VIOLATION` | 30 | 违规退出 |
| `DECEASED` | 40 | 居民死亡 |
| `QUALIFICATION_CANCELLED` | 50 | 资格取消 |
| `HOUSING_ACQUIRED` | 60 | 自购/继承住房 |
| `RENT_ARREARS` | 70 | 欠租 |
| `OTHER` | 90 | 其他 |

#### MemberChangeType — 成员变更类型

| 名称 | code | 标题 |
|---|---|---|
| `ADD_MEMBER` | 10 | 新增成员 |
| `REMOVE_MEMBER` | 20 | 移除成员 |

#### AlertLevel — 预警等级

| 名称 | code | 标题 |
|---|---|---|
| `ALERT_INFO` | 10 | 提示 |
| `ALERT_WARNING` | 20 | 预警 |
| `ALERT_RED` | 30 | 红色预警 |

#### ResidenceType — 居住地址类型

| 名称 | code | 标题 |
|---|---|---|
| `SUBSIDIZED_HOUSING` | 10 | 保障房 |
| `MARKET_RENTAL` | 20 | 市场租赁 |
| `SELF_OWNED` | 30 | 自有住房 |
| `MIGRANT_RENTAL` | 40 | 外出务工租赁 |
| `OTHER` | 90 | 其他 |

#### EmploymentAddressType — 工作地址类型

| 名称 | code | 标题 |
|---|---|---|
| `FIXED_WORKPLACE` | 10 | 固定工作场所 |
| `FLEXIBLE_EMPLOYMENT` | 20 | 灵活就业 |
| `MIGRANT_WORK` | 30 | 外出务工 |
| `OTHER` | 90 | 其他 |

### Struct

#### PrhAddress — 地址

| 属性 | 标题 | 类型 | 必填 |
|---|---|---|---|
| `region` | 行政区划 | `AdministrativeRegion` | ✓ |
| `detail` | 详细地址 | `string` | ✓ |
| `geoPoint` | 定位坐标 | `GeoPoint` | — |

### Class

#### Resident — 保障居民

dataSet: `prh_resident` | extends: `OntologyObject` | implements: `ILogicDelete`, `ITenant`, `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `fullName` | 姓名 | `string` | ✓ | 自有 |
| `idCardNo` | 身份证号 | `string` | ✓ | 自有 |
| `phone` | 手机号 | `string` | — | 自有 |
| `email` | 邮箱 | `string` | — | 自有 |
| `gender` | 性别 | `Gender` | — | 自有 |
| `birthDate` | 出生日期 | `string` | — | 自有 |
| `maritalStatus` | 婚姻状况 | `MaritalStatus` | — | 自有 |
| `guaranteeType` | 保障类型 | `GuaranteeType` | — | 自有 |
| `status` | 居民状态 | `ResidentStatus` | ✓ | 自有 |
| `idCardFrontPhoto` | 身份证正面照片 | `string` | — | 自有 |
| `idCardBackPhoto` | 身份证背面照片 | `string` | — | 自有 |
| `facePhoto` | 人脸照片 | `string` | — | 自有 |
| `householdBookPhoto` | 户口本照片 | `string` | — | 自有 |
| `bankFlowPhoto` | 银行流水照片 | `string` | — | 自有 |
| `marriageCertPhoto` | 婚姻证明照片 | `string` | — | 自有 |
| `incomeCertPhoto` | 收入证明照片 | `string` | — | 自有 |
| `socialSecurityPhoto` | 社保证明照片 | `string` | — | 自有 |
| `user` | 关联系统用户 | `User` | — | 自有 |
| `archiveReason` | 归档原因 | `TerminationReason` | — | 自有 |
| `archiveDate` | 归档日期 | `string` | — | 自有 |
| `archiveNote` | 归档备注 | `string` | — | 自有 |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator` | 创建人 | `string` | — | 来自 IAuditInfo |
| `createAt` | 创建时间 | `string` | — | 来自 IAuditInfo |
| `modifier` | 修改人 | `string` | — | 来自 IAuditInfo |
| `modifyAt` | 修改时间 | `string` | — | 来自 IAuditInfo |
| `id` | 标识 | `string` | — | 继承 OntologyObject |
| `pubts` | 时间戳 | `string` | — | 继承 OntologyObject |

#### Resident 动作

##### add — 新增居民

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Resident"` |
| `payload.fullName` | `string` | ✓ | 姓名 |
| `payload.idCardNo` | `string` | ✓ | 身份证号（唯一校验 + 18位格式校验） |
| `payload.phone` | `string` | ✓ | 手机号（格式校验） |
| `payload.email` | `string` | — | 邮箱 |
| `payload.gender` | `Gender` | — | 性别 |
| `payload.birthDate` | `string` | — | 出生日期 |
| `payload.maritalStatus` | `MaritalStatus` | — | 婚姻状况 |
| `payload.guaranteeType` | `GuaranteeType` | — | 保障类型 |
| `payload.idCardFrontPhoto` | `string` | — | 身份证正面照片 |
| `payload.idCardBackPhoto` | `string` | — | 身份证背面照片 |
| `payload.householdBookPhoto` | `string` | — | 户口本照片 |
| `payload.bankFlowPhoto` | `string` | — | 银行流水照片 |
| `payload.marriageCertPhoto` | `string` | — | 婚姻证明照片 |
| `payload.incomeCertPhoto` | `string` | — | 收入证明照片 |
| `payload.socialSecurityPhoto` | `string` | — | 社保证明照片 |

**Response:** 标准 OntologyResult

##### modify — 修改居民信息

`POST /api/v1/ontology/modify`

**Request Body:** 与 add 类似，所有字段均为可选。需校验身份证号唯一。

**Response:** 标准 OntologyResult

##### verify — 验证

`POST /api/v1/ontology/verify`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Resident"` |
| `payload.facePhoto` | `string` | ✓ | 人脸照片 |

**Response:** 标准 OntologyResult。前置校验：草稿或未认证状态 → 手机验证码 → 人脸比对 → 保存照片 → 设置已认证状态。

##### submit — 提交审核

`POST /api/v1/ontology/submit`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Resident"` |
| `id` | `string` | ✓ | 居民 ID |

**Response:** 标准 OntologyResult

##### activate — 激活居民

`POST /api/v1/ontology/activate`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Resident"` |
| `id` | `string` | ✓ | 居民 ID |

**Response:** 标准 OntologyResult

##### archive — 归档退出

`POST /api/v1/ontology/archive`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Resident"` |
| `payload.archiveReason` | `TerminationReason` | ✓ | 退出原因 |
| `payload.archiveNote` | `string` | — | 备注 |

**Response:** 标准 OntologyResult

##### list / detail

标准查询动作，objectType = `"cn.byteawake.prh.Resident"`

#### Household — 保障家庭

dataSet: `prh_household` | extends: `OntologyObject` | implements: `ILogicDelete`, `ITenant`, `IAuditInfo`, `ISubmitInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `applicant` | 主申请人 | `Resident` | ✓ | 自有 |
| `householdSize` | 家庭人口数 | `number` | ✓ | 自有 |
| `status` | 家庭状态 | `HouseholdStatus` | ✓ | 自有 |
| `guaranteeType` | 保障类型 | `GuaranteeType` | ✓ | 自有 |
| `archiveReason` | 归档原因 | `TerminationReason` | — | 自有 |
| `archiveDate` | 归档日期 | `string` | — | 自有 |
| `archiveNote` | 归档备注 | `string` | — | 自有 |
| `dr` | 逻辑删除 | `boolean` | — | 来自 ILogicDelete |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `submittedAt` | 提交时间 | `string` | — | 来自 ISubmitInfo |
| `submittedBy` | 提交人 | `User` | — | 来自 ISubmitInfo |
| `withdrawnAt` | 撤回时间 | `string` | — | 来自 ISubmitInfo |
| `withdrawnBy` | 撤回人 | `User` | — | 来自 ISubmitInfo |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

链接：`members` → `HouseholdMember[]`

#### Household 动作

##### add — 创建家庭

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Household"` |
| `payload.applicant` | `Resident` | ✓ | 主申请人 ID |
| `payload.householdSize` | `number` | ✓ | 家庭人口数 |
| `payload.guaranteeType` | `GuaranteeType` | ✓ | 保障类型 |

**Response:** 标准 OntologyResult

##### modify — 修改家庭信息

`POST /api/v1/ontology/modify`

**Request Body:** 与 add 类似，所有字段可选。

##### invite — 邀请家庭成员

`POST /api/v1/ontology/invite`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Household"` |
| `payload.resident` | `Resident` | ✓ | 被邀请居民 ID |
| `payload.relation` | `string` | ✓ | 与主申请人关系 |

**Response:** 标准 OntologyResult

##### submit — 提交家庭

`POST /api/v1/ontology/submit`

**Request Body:** objectType + id

##### archive — 归档退出

`POST /api/v1/ontology/archive`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Household"` |
| `payload.archiveReason` | `TerminationReason` | ✓ | 退出原因 |
| `payload.archiveNote` | `string` | — | 备注 |

##### list / detail

标准查询动作

#### HouseholdMember — 家庭成员

dataSet: `prh_household_member` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `household` | 所属家庭 | `Household` | ✓ | 自有 |
| `resident` | 居民 | `Resident` | ✓ | 自有 |
| `relation` | 与主申请人关系 | `string` | ✓ | 自有 |
| `isApplicant` | 是否主申请人 | `boolean` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

无独立动作，通过 Household 的 invite/modify 管理。

#### Residence — 居住信息

dataSet: `prh_residence` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `resident` | 居民 | `Resident` | ✓ | 自有 |
| `residenceType` | 居住类型 | `ResidenceType` | ✓ | 自有 |
| `address` | 地址 | `PrhAddress` | ✓ | 自有 |
| `status` | 记录状态 | `RecordStatus` | — | 自有 |
| `effectiveDate` | 生效日期 | `string` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

#### Residence 动作

##### add — 登记居住信息

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Residence"` |
| `payload.resident` | `Resident` | ✓ | 居民 ID |
| `payload.residenceType` | `ResidenceType` | ✓ | 居住类型 |
| `payload.address` | `PrhAddress` | ✓ | 地址（含 region、detail） |
| `payload.effectiveDate` | `string` | — | 生效日期 |

**Response:** 标准 OntologyResult

##### modify / archive / list / detail

标准动作模式

#### Employment — 工作信息

dataSet: `prh_employment` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `resident` | 居民 | `Resident` | ✓ | 自有 |
| `employer` | 用人单位 | `string` | ✓ | 自有 |
| `employmentType` | 工作类型 | `EmploymentAddressType` | ✓ | 自有 |
| `address` | 工作地址 | `PrhAddress` | — | 自有 |
| `status` | 记录状态 | `RecordStatus` | — | 自有 |
| `effectiveDate` | 生效日期 | `string` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

#### Employment 动作

##### add — 登记工作信息

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.Employment"` |
| `payload.resident` | `Resident` | ✓ | 居民 ID |
| `payload.employer` | `string` | ✓ | 用人单位 |
| `payload.employmentType` | `EmploymentAddressType` | ✓ | 工作类型 |
| `payload.address` | `PrhAddress` | — | 工作地址 |
| `payload.effectiveDate` | `string` | — | 生效日期 |

**Response:** 标准 OntologyResult

##### modify / archive / list / detail

标准动作模式

#### PersonalIncome — 个人收入

dataSet: `prh_personal_income` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `resident` | 居民 | `Resident` | ✓ | 自有 |
| `incomeType` | 收入类型 | `IncomeType` | ✓ | 自有 |
| `monthlyAmount` | 月收入金额 | `number` | ✓ | 自有 |
| `status` | 记录状态 | `RecordStatus` | — | 自有 |
| `effectiveDate` | 生效日期 | `string` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

#### PersonalIncome 动作：add / modify / archive / list / detail

#### EligibilityApplication — 保障资质申请

dataSet: `prh_eligibility_application` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`, `IApprovalFlow`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `household` | 保障家庭 | `Household` | ✓ | 自有 |
| `applicationType` | 申请类型 | `ApplicationType` | ✓ | 自有 |
| `status` | 申请状态 | `ApplicationStatus` | ✓ | 自有 |
| `guaranteeType` | 保障类型 | `GuaranteeType` | ✓ | 自有 |
| `submittedAt` | 提交时间 | `string` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `approvalFlowEnabled`~`task` | 审批流信息 | — | — | 来自 IApprovalFlow |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

#### EligibilityApplication 动作

##### add — 新增保障申请

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.EligibilityApplication"` |
| `payload.household` | `Household` | ✓ | 保障家庭 ID |
| `payload.applicationType` | `ApplicationType` | ✓ | 申请类型 |
| `payload.guaranteeType` | `GuaranteeType` | ✓ | 保障类型 |

**Response:** 标准 OntologyResult

##### modify — 修改保障申请

`POST /api/v1/ontology/modify`

##### submit — 提交保障申请

`POST /api/v1/ontology/submit`

**Request Body:** objectType + id

##### unsubmit — 撤回保障申请

`POST /api/v1/ontology/unsubmit`

**Request Body:** objectType + id

##### list / detail

标准查询动作

#### HousingAllocation — 实物配租

dataSet: `prh_housing_allocation` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`, `IApprovalFlow`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `household` | 保障家庭 | `Household` | ✓ | 自有 |
| `residence` | 配租房源 | `Residence` | — | 自有 |
| `status` | 分配状态 | `AllocationStatus` | ✓ | 自有 |
| `allocationDate` | 配租日期 | `string` | — | 自有 |
| `startDate` | 起租日期 | `string` | ✓ | 自有 |
| `endDate` | 到期日期 | `string` | ✓ | 自有 |
| `monthlyRent` | 月租金 | `number` | — | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `approvalFlowEnabled`~`task` | 审批流信息 | — | — | 来自 IApprovalFlow |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

#### HousingAllocation 动作

##### add — 新增配租

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.HousingAllocation"` |
| `payload.household` | `Household` | ✓ | 保障家庭 ID |
| `payload.startDate` | `string` | ✓ | 起租日期 |
| `payload.endDate` | `string` | ✓ | 到期日期 |
| `payload.monthlyRent` | `number` | — | 月租金 |

##### submit — 提交生效

`POST /api/v1/ontology/submit`

##### unsubmit — 撤回配租

`POST /api/v1/ontology/unsubmit`

##### terminate — 终止配租

`POST /api/v1/ontology/terminate`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.HousingAllocation"` |
| `payload.archiveReason` | `TerminationReason` | ✓ | 终止原因 |
| `payload.archiveNote` | `string` | — | 备注 |

##### expire — 配租到期

`POST /api/v1/ontology/expire`

##### list / detail

标准查询动作

#### RentalSubsidy — 租赁补贴

dataSet: `prh_rental_subsidy` | extends: `OntologyObject` | implements: `ITenant`, `IAuditInfo`, `IApprovalFlow`

| 属性 | 标题 | 类型 | 必填 | 来源 |
|---|---|---|---|---|
| `household` | 保障家庭 | `Household` | ✓ | 自有 |
| `status` | 补贴状态 | `SubsidyStatus` | ✓ | 自有 |
| `monthlyAmount` | 月补贴金额 | `number` | ✓ | 自有 |
| `startDate` | 起始日期 | `string` | ✓ | 自有 |
| `endDate` | 截止日期 | `string` | ✓ | 自有 |
| `tenant` | 租户 | `string` | ✓ | 来自 ITenant |
| `creator`~`modifyAt` | 审计信息 | — | — | 来自 IAuditInfo |
| `approvalFlowEnabled`~`task` | 审批流信息 | — | — | 来自 IApprovalFlow |
| `id` / `pubts` | 基础字段 | — | — | 继承 OntologyObject |

#### RentalSubsidy 动作

##### add — 新增补贴

`POST /api/v1/ontology/add`

**Request Body:**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `objectType` | `string` | ✓ | `"cn.byteawake.prh.RentalSubsidy"` |
| `payload.household` | `Household` | ✓ | 保障家庭 ID |
| `payload.monthlyAmount` | `number` | ✓ | 月补贴金额（正数） |
| `payload.startDate` | `string` | ✓ | 起始日期 |
| `payload.endDate` | `string` | ✓ | 截止日期 |

##### submit — 提交生效

`POST /api/v1/ontology/submit`

##### suspend — 暂停补贴

`POST /api/v1/ontology/suspend`

##### terminate — 终止补贴

`POST /api/v1/ontology/terminate`

##### expire — 补贴到期

`POST /api/v1/ontology/expire`

##### list / detail

标准查询动作

---
## 实战案例

### 案例1：注册 + 登录

```typescript
// 1. 注册
const signupRes = await fetch('/api/v1/ontology/signup', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    objectType: 'cn.byteawake.ap.arche.User',
    payload: {
      account: 'zhangsan',
      email: 'zhangsan@example.com',
      password: 'Str0ng!Pass',
      confirmPassword: 'Str0ng!Pass',
    },
  }),
});
const signupData = await signupRes.json();
// signupData.data[0].sessionId → 用于后续请求

// 2. 密码登录
const authRes = await fetch('/api/v1/ontology/authenticate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    objectType: 'cn.byteawake.ap.arche.User',
    payload: {
      account: 'zhangsan',
      password: 'Str0ng!Pass',
    },
  }),
});
```

### 案例2：新增保障居民并激活

```typescript
// 1. 新增居民（需 PRH 管理员权限）
const addRes = await fetch('/api/v1/ontology/add', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.Resident',
    payload: {
      fullName: '张三',
      idCardNo: '110101199001011234',
      phone: '13800138000',
      gender: 'MALE',
      guaranteeType: 'NEW_EMPLOYEE',
    },
  }),
});

// 2. 人脸验证
const verifyRes = await fetch('/api/v1/ontology/verify', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.Resident',
    payload: { facePhoto: 'base64...' },
  }),
});

// 3. 激活
const activateRes = await fetch('/api/v1/ontology/activate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.Resident',
    id: residentId,
  }),
});
```

### 案例3：创建保障家庭并申请保障

```typescript
// 1. 创建家庭
const householdRes = await fetch('/api/v1/ontology/add', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.Household',
    payload: {
      applicant: residentId,
      householdSize: 3,
      guaranteeType: 'LOW_INCOME',
    },
  }),
});

// 2. 邀请家庭成员
const inviteRes = await fetch('/api/v1/ontology/invite', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.Household',
    payload: { resident: memberResidentId, relation: '配偶' },
  }),
});

// 3. 提交家庭
await fetch('/api/v1/ontology/submit', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.Household',
    id: householdId,
  }),
});

// 4. 新增保障申请
const appRes = await fetch('/api/v1/ontology/add', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.EligibilityApplication',
    payload: {
      household: householdId,
      applicationType: 'INITIAL',
      guaranteeType: 'LOW_INCOME',
    },
  }),
});

// 5. 提交保障申请
await fetch('/api/v1/ontology/submit', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.EligibilityApplication',
    id: applicationId,
  }),
});
```

### 案例4：分页查询 + 条件过滤

```typescript
const listRes = await fetch('/api/v1/ontology/list', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.prh.Resident',
    payload: {
      selection: 'fullName,idCardNo,status,guaranteeType',
      filter: {
        operator: 'AND',
        conditions: [
          { field: 'status', operator: 'EQ', value1: 'ACTIVATED' },
          { field: 'guaranteeType', operator: 'IN', values: ['LOW_INCOME', 'MIDDLE_INCOME'] },
        ],
      },
      sortItems: [{ path: 'fullName', direction: 'ASC' }],
      page: { pageNo: 1, pageSize: 20 },
    },
  }),
});
const data = await listRes.json();
// data.data → Resident[]
// data.page.total → 总记录数
```

### 案例5：树查询行政区划

```typescript
const treeRes = await fetch('/api/v1/ontology/querytree', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({
    objectType: 'cn.byteawake.ap.basedoc.AdministrativeRegion',
    payload: {
      selection: 'code,name,regionType',
      filter: {
        operator: 'AND',
        conditions: [
          { field: 'enable', operator: 'EQ', value1: 'true' },
          { field: 'dr', operator: 'EQ', value1: 'false' },
        ],
      },
      treeOptions: {
        rootId: null,
        scope: 'SUBTREE',
        recursive: true,
        maxDepth: 3,
      },
    },
  }),
});
```

### 案例6：标准错误处理

```typescript
async function callOntology(actionName: string, body: object) {
  const res = await fetch(`/api/v1/ontology/${actionName}`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${token}`,
    },
    body: JSON.stringify(body),
  });
  const result = await res.json();

  if (!result.success) {
    if (result.fieldErrors?.length) {
      // 字段级校验错误
      for (const err of result.fieldErrors) {
        console.error(`${err.path}: ${err.message}`);
      }
    } else {
      // 业务级错误
      console.error(`[${result.code}] ${result.message}`);
    }
    return null;
  }
  return result.data;
}
```
